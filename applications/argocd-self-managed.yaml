apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  # Multi-source: chart do Helm repo + values do Git repo
  sources:
    # Fonte 1: Helm chart oficial do Argo CD
    - repoURL: https://argoproj.github.io/argo-helm
      chart: argo-cd
      targetRevision: 5.32.0
      helm:
        releaseName: argocd
        valueFiles:
          # Referência ao arquivo do Git (via $values abaixo)
          - $values/helm-values/base-values.yaml

    # Fonte 2: Git repo com os values
    - repoURL: https://github.com/westibcar/argocd-migration.git
      targetRevision: HEAD
      ref: values  # alias usado no valueFiles acima como $values

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
